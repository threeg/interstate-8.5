# Version Brief: Interstate-8 v5 — `5.0.x-dev2` (slice 2)

| | |
|---|---|
| **Document** | Version brief (delta pass) |
| **Repository location** | `spec/brief/5.0.x-dev2-brief.md` |
| **Status** | **Draft** — laid down by `sfk-version` 2026-08-02; ratified at Milestone 10 |
| **Version** | `5.0.x-dev2` — the second slice of the rolling `5.0.x-dev` line |
| **Previous** | `5.0.x-dev` slice 1, signed off and tagged `5.0.x-dev1` (2026-08-02) |
| **Northstar** | `spec/brief/brief.md` — unchanged and still binding |

> **This is a delta brief.** It scopes slice 2 as **changes against the living spec**, not as a fresh
> specification. `spec/brief/brief.md` remains the binding project brief; nothing here reopens it.
> What follows becomes new `FR`/`NFR` numbers and in-place amendments during Milestones 11–15.
>
> **Draft status is deliberate.** `sfk-version` writes this; Milestone 10 reviews and ratifies it. The
> milestone is `Not started` even though the file exists — the draft existing is not progress.

---

## 1. Why this version exists

Slice 1's real job was a **viability test**: could Claude Design produce a good enough look to break the
design stall that killed v3 and v4? It answered yes. But nine milestones of building against that answer
taught us things the slice-1 specification could not have known, and two of them are now parked decisions
(`TODO-001`, `TODO-002`) with a third — how the design sources are *organised* — visible only in
hindsight.

Slice 2 is therefore a **short consolidation slice, not a new content type.** It finishes the two screens
slice 1 actually designed (the homepage and the song page), fixes the design-source structure before that
structure is copied into every future slice, and closes the reproducibility gap. Slice 3 opens the next
content type on a clean base.

**The cost of not doing this now is asymmetric.** Every one of the four scope items gets more expensive
with each slice added on top: a design-source layout is copied by the next section, a reproducibility gap
widens with every content entity added, and a song-page composition that "does not work" becomes the
pattern later pages imitate. None of them gets cheaper by waiting.

## 2. Goals

Slice 2 delivers four things. **A and B are new behaviour** (new `FR` numbers); **C and D are corrections
to slice 1's output** (largely amend-in-place).

### A. Song page — alternate-version composition (resolves `TODO-001`)

`INT8-020` shipped `FR-13`/`FR-20` as specified and its 17 Playwright assertions pass, but the result was
judged unsatisfying on review. The hi-fi only ever draws the lyric-pair **as an isolated panel**, never
inside the full song-page layout, so *where it sits in the page* was never actually decided by the design.

Slice 2 makes that decision and rebuilds to it, resolving the three things `TODO-001` names: where the
pair sits relative to the quote, lyrics, notes and right-hand rail; whether it keeps the panel framing
(border + `--color-tint` header bar) or dissolves into the page; and how the parent cross-link and the
"alternate versions" list relate to it.

**Reuse, not restart.** The `lyric-pair` SDC is deliberately structural (two slots, no props), the
song-versions resolution already lives in `i8_services` (`INT8-035`), and `song-versions.spec.ts` asserts
*behaviour* (`FR-13`/`FR-20`) rather than layout — only its side-by-side/stacking checks are geometry-
specific. A redesign should need to revisit those and little else.

### B. Homepage hero + primary nav (new)

A **second hero component**, homepage-only, sitting alongside the existing `page_hero`:

- It is a block, like the existing one, but displays a **set message** rather than a page title.
- It draws from a **library of images**, shown at random rather than fixed per page.
- The **primary nav must work over it** — transparent while the hero is in view, solid on scroll.

**The nav half is smaller than it looks.** `site-header.js` already implements exactly this: a
`site-header--transparent` variant that toggles `is-scrolled` past 24px, with
`--color-nav-hover-on-transparent` already in the token set for the hover state it needs. Slice 2's work
there is to **wire the new hero to the existing variant and prove it** with a front-end test, not to
build the behaviour. The requirements delta should say so, so a ticket is not written for work already
done.

**This is not the homepage build.** The rest of the homepage stays out of scope (§5) — this closes the
two components slice 1 designed and left unbuilt, nothing more.

### C. Install-required content is reproducible (resolves `TODO-002`)

Three pieces of the shipped site live only in the development database: the primary nav's **Home** and
**Songs** links (the Songs one being the concrete realisation of `FR-16`), the five footer labels, and
the `page_hero` background block — whose UUID `block.block.interstate_85_pageherobackground.yml`
hard-references in both `dependencies.content` and its `plugin` key. A fresh `site-install` +
`config:import` produces a site with no primary navigation, an empty footer row and a broken hero block,
**while every gate stays green**, because `lando test` and `lando playwright` both run against the
existing database.

`TODO-002` left two things open and slice 2 answers both: **which mechanism** captures this content
(`default_content`, an owned `hook_install()` in `i8_services`, a content export/import step, or
documented manual steps), and **whether it is in scope at all** given there is still no deploy target.

**Goal B makes this urgent rather than theoretical.** The image library and the homepage hero block are
*more* install-required content, on the same footing as the three entities already missing. Adding them
without deciding the mechanism widens a known gap by design.

### D. Design sources restructured (new — process correction)

Slice 1 put all three screens, the component library and the token panel into a **single Claude Design
page**, exported as one file (`Interstate-8 1B.dc.html`). The **authority rules** written around it
(`design-system.md` §1.1) are sound and stay; the **file layout** does not scale and is replaced:

1. **One design page per major page/section**, exported one file per page — `song-page.dc.html`,
   `release-landing.dc.html` and so on — each the sole source of truth for the page it names.
2. **A dedicated tokens page** and **a dedicated components page**, so a component's shape is defined
   once rather than redrawn inside every page that uses it.
3. **Wireframes feed design; design feeds code.** Wires are a quick, low-fidelity way to agree
   *placement*; implementation is built from the design file only, never from a wire.

**Why one file per page is the right call.** Four reasons, in order of weight for this project:

- **Slices add pages.** Under the current layout, slice 3 must edit the one file every prior slice
  depends on. Under the new one it adds a file. That is the difference between a design source that
  scales with the delivery model and one that fights it.
- **Reviewable diffs.** A change to the song page currently rewrites the file the songs landing also
  binds to, so no diff can show which page changed.
- **Freezing.** A shipped page's design cannot be held stable while another iterates when both live in
  one artefact.
- **Citable authority.** A ticket's `## Design authority` currently cites one file plus a screen name;
  a reader cannot verify they read the same region twice. A filename is checkable.

**The risk this introduces, and the rule that contains it.** Standalone page files duplicate components,
and duplicated components drift. The components page is what prevents that, but only with an explicit
precedence rule: **the components page is authoritative for a component's shape and states; a page file
is authoritative for composition and placement only.** Without that, the same button exists in six files
in six shapes and nothing says which is right. Milestone 14 must write this rule, not just produce the
files.

## 3. Decisions this version owes

Recorded here so the milestone that owns each one cannot skip it. These are **not** open questions in the
`spec/open-questions.md` sense — they are ours to make, and slice 2 exists to make them.

| # | Decision | Owed by |
|---|----------|---------|
| D-a | Where the alternate-version pair sits in the song page; panel framing kept or dissolved; how the parent cross-link relates (`TODO-001`) | M13 wireframes → M14 design |
| D-b | The homepage hero's content model — block type, the message field, and how the image library is modelled (media entities? a media field? a curated set?) | M12 architecture |
| D-c | Random selection semantics: per page load, per session, or per cache lifetime — and what that means for Drupal's render cache | M11 requirements → M12 architecture |
| D-d | The install-required content mechanism, and whether it is slice-2 scope (`TODO-002`) | M12 architecture |
| D-e | Whether wireframes stay **binding for state coverage** or become purely non-binding inputs to design | M13 wireframes |
| D-f | Which artefact binds token values — `tokens.css` or the new design tokens page | M14 design |
| D-g | The components-page precedence rule (§2 D above), written into `design-system.md` §1.1 | M14 design |

**A recommendation on D-e and D-f, for the milestone to accept or reject.** `design-system.md` §1.1
already says wireframes are "binding for structure and state coverage… not a value source" while the
hi-fi binds "placement, **structure**, hierarchy" — the word *structure* appears in both rows, and that
overlap is exactly the ambiguity that lets a wire be read as an implementation reference. The cheap fix
is to sharpen the split (wires bind *which surfaces and states exist*; the design file binds *everything
about how they look*) rather than to demote wireframes entirely, because a hi-fi rarely draws every
empty, loading and error state and something has to. On D-f, `tokens.css` should keep winning: the theme
imports it, so a second source would be a source that code does not read.

## 4. Success criteria

Slice 2 is done when:

1. The song page's alternate-version view matches a design that draws it **inside the real page**, and
   `song-versions.spec.ts` still asserts `FR-13`/`FR-20` behaviour.
2. The homepage renders the new hero with its set message and an image drawn from the library, with the
   primary nav transparent over it and solid on scroll — covered by a Playwright test.
3. A fresh `site-install` + config import produces a site with working primary navigation, the footer
   labels and an intact hero block — by whatever mechanism M12 selects — **or** the decision to defer is
   recorded in the spec with its reasoning, and `TODO-002` is tombstoned either way.
4. `spec/design/` holds one design file per page, a tokens page and a components page, with
   `design-system.md` §1 and §1.1 amended to match, the superseded `Interstate-8 1B.dc.html` marked as
   such rather than deleted (existing tickets cite it), and root `CLAUDE.md`'s *Where things live* updated.
5. `lando test` and `lando playwright` both green; `INT8-047` closed.

## 5. Out of scope

- **The rest of the homepage** — only the hero and the nav over it. No news row, no contribute panel, no
  discography strip.
- **Any new content type** — Releases, Concerts/Setlists and News all stay for slice 3+.
- **Cross-entity relationships on songs** — the seams stay nullable and unpopulated.
- **Search API / faceted search** — still deferred, still no trigger.
- **setlist.fm integration** — still deferred.
- **Re-doing slice 1's designs wholesale.** D is a *restructure* of the sources plus the two pages named
  in A and B. Existing screens are migrated into the new layout as they are, not redrawn.

## 6. Carried in from slice 1

- **`INT8-047`** — reconcile six record drifts left by the `INT8-043`–`046` batch. Docs-only, still
  `todo`, re-milestoned into slice 2 at ticket generation (M16).
- **`S-1`** — the `song_type` equal-weight tie-break. Left open deliberately: no external dependency, so
  **no lead time**, and unreachable on today's four types. Nothing in slice 2 adds a fifth type. Revisit
  when one is added, not now.
- **No `Q-n` rows** — slice 1 built against a v2 database the project owns outright, so there is nobody
  outside the team we are waiting on. Nothing in slice 2's scope changes that.

## 7. Risks

| Risk | Notes / mitigation |
|------|--------------------|
| **Scope creep into "the homepage"** | The hero is one component; the homepage is a page. §5 draws the line and the requirements delta must keep it — if M11 starts numbering `FR`s for a news row, the line has moved. |
| **The design restructure becomes a redraw** | Migrate existing screens as-is (§5). Only the song page and the homepage hero are *designed* in slice 2; everything else is a move. |
| **Component drift across per-page design files** | The components-page precedence rule (D-g) is a required deliverable of M14, not a nice-to-have. Without it the restructure trades one problem for a worse one. |
| **Stale citations after the split** | `Interstate-8 1B.dc.html` is cited by shipped tickets. It is marked superseded and kept, per the project's demote-don't-erase convention (`spec/TODO.md`, `BOARD.md`, `open-questions.md` all work this way). |
| **`TODO-002` over-engineered** | Lazy adoption still applies: there is no deploy target. "Documented manual steps + a test that would catch it" is a legitimate answer, and deferring with recorded reasoning is an acceptable outcome for criterion 3. |
| **Random images defeat render caching** | D-c is a requirements decision precisely so this is settled before implementation, rather than discovered when the hero shows the same image to everyone for a day. |

---

*Ratification of this brief closes Milestone 10.*
